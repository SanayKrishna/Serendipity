# Example backend .env.example — DO NOT store secrets in version control
# Use this as a template and fill the real values in a local `.env` file

# Single DATABASE_URL (preferred for deployments)
# DATABASE_URL=postgresql://<user>:<password>@<host>:<port>/<db_name>

# Or set individual values:
DB_HOST=aws-1-ap-southeast-1.pooler.supabase.com
DB_PORT=6543
DB_USER=postgres.frbtmavtrqnjwqsinyej
DB_PASSWORD=<YOUR_DB_PASSWORD_HERE>
DB_NAME=postgres

ENVIRONMENT=production
DEBUG=false
CORS_ORIGINS=*
